services:
  portal:
    image: nginx:alpine
    container_name: cka-psi-portal
    restart: unless-stopped
    ports:
      - "8090:80"
      - "8091:443"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl:/etc/nginx/ssl:ro

apiVersion: v1
kind: Service
metadata:
  labels:
    app: service-blue
  name: blue-svc
  namespace: ingress-lab
spec:
  ports:
    - name: "80"
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: service-blue
  type: ClusterIP
status:
  loadBalancer: {}
